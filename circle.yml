machine:
  services:
    - docker
  hosts:
    s2.testspace-qa.com: 34.213.188.166

dependencies:
  pre:
    - curl -s https://testspace-client.s3.amazonaws.com/testspace-linux-dev.tgz | tar -zxvf- -C $HOME/bin
    - testspace config url http://$S2_TESTSPACE@s2.testspace-qa.com/sandio:testspace.test.ci
    - testspace -v

test:
  override:
    - printenv > printenv.txt

  post:
    - testspace results.xml printenv.txt "standalone#circle.$CIRCLE_BUILD_NUM" --repo git
