machine:
  services:
    - docker
  hosts:
    s2.testspace-qa.com: ${qa-ip}

general:
  branches:
    only:
      - standalone
      - circle

dependencies:
  pre:
    - curl -s https://testspace-client.s3.amazonaws.com/testspace-linux-dev.tgz | tar -zxvf- -C $HOME/bin
    - testspace config url http://499a20c107b8fd5888ec3b3a86c9099523162f4d:@s2.testspace-qa.com
    - testspace -v
    
test:
  override:
    - echo test override
    
  post:
    - ./incremental.sh 
